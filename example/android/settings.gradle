pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    plugins {
        id "dev.flutter.flutter-gradle-plugin" version "1.0.0"
        id "org.jetbrains.kotlin.android" version "1.9.0"
    }
}

include(":app")
